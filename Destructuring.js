const destructuring = {
  name: "Dima",
  yeras: 32,
  gender_male: "man",
};

const { name, yeras } = destructuring;
const { gender_male } = destructuring;

console.log(name);
console.log(yeras);
console.log(gender_male);
