const value = 16;

const functionIF = (value) => {
  if ((value) => 4) {
    value += 11;
    return value;
  }
  return console.log(false);
};

console.log(functionIF(value));

// other example
const ifFunctions = {
  name: "Dima",
};

const functionExample = (params) => {
  if (!ifFunctions.age) {
    return (age = 25);
  }
  return console.log(false);
};

console.log(functionExample());
